# TODO.md

## Next steps

- dedicated runner groups
- split trusted/untrusted runner pools
- optional repo allow-listing
- pinned image tags instead of `latest`
- preinstalled common linters/tools in the runner image
- a `Makefile` for local operations
